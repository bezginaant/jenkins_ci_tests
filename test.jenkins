pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'Building...'
                echo 'Build completed'
            }
        }
        stage('testing') {
            steps {
                sh 'cd ../pytest_homework'
                sh 'pip install -r requirements.txt'
            }
        }
    }
}
