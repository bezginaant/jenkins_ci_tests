pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'Building...'
                echo 'Build completed'
            }
        }
        stage('testing') {
            steps {
                sh 'pwd'
                sh "python3 --version"
                sh "git clone https://github.com/bezginaant/jenkins_ci_tests.git"
                sh 'pwd'
                sh 'ls'
            }
        }
    }
}
